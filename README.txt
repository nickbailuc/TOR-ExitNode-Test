4 hours and 9 minutes are required to ping all 249 regions (60 seconds per exit node)
This script by default will return the list of regions to stdout.
Use \"./tor-exitnode-test.sh > file.txt\" to output to file.txt insead.
Make sure the tor package is installed, and port 9050 is not occupied by any proess!
